# Environment setup

Build the plain docker container...

    bash docker/build.sh

Then drop into a docker environment 

    bash docker/ruh.sh



# Environment setup

We use docker for this.

    docker build -t coq-tactician-api

Then 

    docker run  coq-tactician-api bash

# Build out simple test

For this, we will just build a simple thing to sanity check out that the pipeline works as expected.
